bitmagnet.io/schemas/**/*.*
webui/dist/**/*.*
webui/src/app/graphql/generated/**/*.*
webui/src/app/i18n/translations/*.json
webui/.angular

# Untracked local files. These are gitignored, but Prettier reads .prettierignore
# rather than .gitignore, so without these rules `task lint` fails on whatever the
# operator happens to have in their checkout. Keep in step with .gitignore.
/.agents
/.claude
/skills-lock.json
/docs/agents/
*.local.md
