package search

// Column names this package orders and aliases by. Named because the same
// literals appear across several order and search definitions.
const (
	colInfoHash    = "info_hash"
	colName        = "name"
	colPublishedAt = "published_at"
	colSize        = "size"
)
